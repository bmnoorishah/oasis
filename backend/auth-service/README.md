# Auth Service

Simple authentication microservice (starter).

Endpoints:
- GET /health
- POST /login
- POST /logout

Run locally:
1. cp .env.example .env and update values
2. npm install
3. npm run dev
